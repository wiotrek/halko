import { Dictionary } from 'src/app/shared/models/dictionary.model';
import { faArrowDown, faArrowUp, IconDefinition, } from '@fortawesome/free-solid-svg-icons';

export const SearcherVectorIconsDictionary: Dictionary<IconDefinition> = {
  up: faArrowUp,
  down: faArrowDown,
};
